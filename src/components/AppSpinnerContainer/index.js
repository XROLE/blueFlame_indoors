import React, { Component } from 'react';
import Spinner from '../Spinner';
import './appSpinnerContainer.scss';

export default () => (
  <div className="app-spinner-container">
    <Spinner />
  </div>
)
